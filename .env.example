# Optional Supabase sync (M3.2). Leave unset to keep offline-only.
# Tables: sessions (id uuid PK, started_at, play_mode, curriculum_id, scoring_mode)
#         events (session_id, player, level, question_text, expected_answer,
#                 skill_tags jsonb, response_ms, correct, attempt, created_at)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
